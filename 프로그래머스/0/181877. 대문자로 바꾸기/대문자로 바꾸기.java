class Solution {
    
    public String solution(String myString) {
        
        // 문자열을 모두 대문자로 변환 (예: "hello" -> "HELLO")
        String answer = myString.toUpperCase();
        
        return answer;
    }
}
